{
  "name": "max-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "connectors/*",
    "apps/*",
    "examples"
  ],
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test --continue"
  },
  "devDependencies": {
    "@max/connector-acme": "workspace:*",
    "@max/federation": "workspace:*",
    "@max/core": "workspace:*",
    "@max/execution": "workspace:*",
    "@max/execution-local": "workspace:*",
    "@max/execution-sqlite": "workspace:*",
    "@max/storage-sqlite": "workspace:*",
    "@types/bun": "latest",
    "prettier": "^3.8.1",
    "turbo": "^2.8.6",
    "typescript": "catalog:"
  },
  "catalog": {
    "typescript": "5.9.3"
  },
  "packageManager": "bun@1.3.9",
  "patchedDependencies": {
    "@optique/core@0.10.6": "patches/@optique%2Fcore@0.10.6.patch"
  }
}
