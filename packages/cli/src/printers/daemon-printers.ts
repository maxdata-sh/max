/**
 * Daemon printers — temporarily disabled during federation migration.
 *
 * The FsProjectDaemonManager has been retired. Daemon status rendering
 * will be re-implemented when GlobalMax + workspace supervisor replaces it.
 */

// TODO: Re-implement with workspace registry entries once GlobalMax integration is complete.
export const DaemonPrinters = {}
