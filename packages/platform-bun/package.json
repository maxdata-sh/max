{
  "name": "@max/platform-bun",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    "types": "./src/index.ts",
    "default": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@max/federation": "workspace:*",
    "@max/core": "workspace:*",
    "@max/connector": "workspace:*",
    "@max/execution": "workspace:*",
    "@max/execution-local": "workspace:*",
    "@max/execution-sqlite": "workspace:*",
    "@max/storage-sqlite": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "catalog:",
    "@max/connector-acme": "workspace:*"
  }
}
